{
  "name": "requests-demo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Roku showcase for flash-theater's request Http {} surface — declaration/calling, response caching, and buildRequest/parseResponse/parseError crash safety — split off apps/async-demo as this repo's dedicated, router-mounted chapter app for the requests doc-nav topic (see findings/demo-app-conventions.md).",
  "scripts": {
    "compile": "flash-theater compile",
    "zip": "flash-theater zip",
    "build:roku": "npm run compile && npm run zip",
    "sideload": "kopytko-roku installer install --zip dist/requests-demo.zip"
  },
  "devDependencies": {
    "flash-theater-compiler": "*",
    "kopytko-roku-device": "^1.5.1"
  }
}
