{
  "name": "focus-demo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Roku testbed for flash-theater's focus/on:key system, isolated from every other sample-app demo so FlashTheaterFocusManager's app-wide LRUD registry has only ScrollFocusDemo's own tiles to navigate.",
  "scripts": {
    "compile": "flash-theater compile",
    "zip": "flash-theater zip",
    "build:roku": "npm run compile && npm run zip",
    "sideload": "kopytko-roku installer install --zip dist/focus-demo.zip"
  },
  "devDependencies": {
    "flash-theater-compiler": "*",
    "kopytko-roku-device": "^1.5.1"
  }
}
