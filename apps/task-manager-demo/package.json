{
  "name": "task-manager-demo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Roku showcase for flash-theater's taskManager surface — run/cancel/priority queueing, setMaxConcurrent, hysteresis-gated alerting (onAlertChanged), promise-style onResult, and the global onRequestSent/onResponseReceived interceptors — split off apps/async-demo as this topic's own dedicated, router-mounted chapter app (see findings/demo-app-conventions.md).",
  "scripts": {
    "compile": "flash-theater compile",
    "zip": "flash-theater zip",
    "build:roku": "npm run compile && npm run zip",
    "sideload": "kopytko-roku installer install --zip dist/task-manager-demo.zip"
  },
  "devDependencies": {
    "flash-theater-compiler": "*",
    "kopytko-roku-device": "^1.5.1"
  }
}
