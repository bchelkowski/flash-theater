Placeholder asset that only ever ships in the "dev" environment build — see
apps/environments-demo/environments/dev.config.json's "include" and the base
flash-theater.config.json's "exclude" for how this file is kept out of the "prod"
build. A plain text placeholder (not a real image) is enough to prove the
include/exclude mechanics, since this file isn't referenced from any .thr
template or the manifest. See findings/environments.md and
findings/environments-demo-app.md.
