{
  "name": "statements-demo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Roku showcase for flash-theater's statement/expression grammar — if/else if/else (block + inline), ternary, crash-safe ==/!=/</>/<=/>=/!, automatic chain safety, for/for each/while loops, anonymous function expressions, and the raw BrightScript passthrough escape hatch — this topic's own dedicated, router-mounted chapter app (see findings/demo-app-conventions.md).",
  "scripts": {
    "compile": "flash-theater compile",
    "zip": "flash-theater zip",
    "build:roku": "npm run compile && npm run zip",
    "sideload": "kopytko-roku installer install --zip dist/statements-demo.zip"
  },
  "devDependencies": {
    "flash-theater-compiler": "*",
    "kopytko-roku-device": "^1.5.1"
  }
}
