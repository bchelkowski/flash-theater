{
  "name": "theme-demo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Roku showcase for flash-theater's theme surface — <theme-template>/<theme name=\"...\"> declaration and partial-override validation, bare theme.a.b access from derived and template bindings, and runtime switchTheme(name) — this topic's own dedicated, router-mounted chapter app (see findings/demo-app-conventions.md).",
  "scripts": {
    "compile": "flash-theater compile",
    "zip": "flash-theater zip",
    "build:roku": "npm run compile && npm run zip",
    "sideload": "kopytko-roku installer install --zip dist/theme-demo.zip"
  },
  "devDependencies": {
    "flash-theater-compiler": "*",
    "kopytko-roku-device": "^1.5.1"
  }
}
